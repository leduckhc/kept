# Prevent accidental npm usage — this project uses pnpm
engine-strict=true

# Block install scripts globally (allow via pnpm-workspace.yaml only)
ignore-scripts=true

# 3-day cooldown before new versions can be installed
min-release-age=3
