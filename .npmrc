# This project uses pnpm exclusively — npm/yarn are blocked.
engine-strict=true

# Packages must be at least 3 days old before install (supply-chain hardening)
min-release-age=3d

# Use pnpm-workspace.yaml allowBuilds for script control (not blanket ignore)
confirm-modules-purge=false
