# Kept MVP Demo

1. Open the desktop preview with `npm run dev`.
2. Use the seeded local demo query: `invoice next week`.
3. Confirm results come from local sample threads.
4. Open the privacy card and verify AI is off by default.
5. For BYO AI, show the prompt audit before any content would be sent.

Demo data is synthetic until the Gmail connector lands. Real account ingestion must be opt-in and local-only.
